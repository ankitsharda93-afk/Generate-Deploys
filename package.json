{
  "name": "sheet-to-pages",
  "private": true,
  "engines": {
    "node": ">=20 <24"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "predev": "npx kill-port --port 3000,4000",
    "dev": "concurrently -n WEB,API -c blue,green \"npm:dev -w apps/web\" \"npm:dev -w apps/api\"",
    "predev:stable": "npx kill-port --port 3000,4000",
    "dev:stable": "concurrently -n WEB,API -c blue,green \"npm run dev:stable -w apps/web\" \"npm:dev -w apps/api\"",
    "build": "npm run build -w apps/web && npm run build -w apps/api",
    "lint": "npm run lint -w apps/web && npm run lint -w apps/api"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "kill-port": "^2.0.1",
    "wrangler": "^4.8.0"
  }
}
